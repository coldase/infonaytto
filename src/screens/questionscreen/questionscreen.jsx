//Import styles
import { Navigate } from "react-router-dom";
import "./questionscreen.css";

const QuestionScreen = ({ currentUser }) => {
  return (
    <div>
      <h1 className="asd">questionscreen</h1>
    </div>
  );
};

export default QuestionScreen;
